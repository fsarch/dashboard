# patch-package

patch-package has to be used for `@point-of-sale/webusb-receipt-printer` to fix the import paths for non-browser environments (e.g. Next.js).

```npx patch-package @point-of-sale/webusb-receipt-printer --exclude 'nothing'```
